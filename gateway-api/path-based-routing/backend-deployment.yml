apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment-path-based
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-deployment-path-based
  template:
    metadata:
      labels:
        app: backend-deployment-path-based
    spec:
      containers:
        - image: hashicorp/http-echo
          name: backend-deployment-path-based
          args:
            - "-text=Backend Deployment Path Based"
