apiVersion: v1
kind: Service
metadata:
  name: frontend-service-path-based
spec:
  selector:
    app: frontend-service-path-based
  ports:
    - port: 5678
      targetPort: 5678
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service-path-based
spec:
  selector:
    app: backend-service-path-based
  ports:
    - port: 5678
      targetPort: 5678
