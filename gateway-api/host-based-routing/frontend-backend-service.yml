apiVersion: v1
kind: Service
metadata:
  name: frontend-service-host-based
spec:
  selector:
    app: frontend-deployment-host-based
  ports:
    - port: 5678
      targetPort: 5678
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service-host-based
spec:
  selector:
    app: backend-deployment-host-based
  ports:
    - port: 5678
      targetPort: 5678
